import { CheckCircle2, CircleAlert, ShieldCheck } from 'lucide-react'
import { PageFrame, SectionHeading } from '@/components/app-header'
import { phaseNineTenChecklist } from '@/lib/release/checklist'

export default function ReleaseStatusPage() {
  const passed = phaseNineTenChecklist.filter(item => item.status === 'pass').length
  return <PageFrame active="System Health"><div className="mx-auto max-w-5xl"><SectionHeading eyebrow="Release engineering · transparent gates" title="Release status" description="A public, auditable view of what is ready, pending, or blocked before production release." /><div className="mt-8 grid gap-4 sm:grid-cols-3"><div className="rounded-2xl border border-[#71d7ba]/20 bg-[#71d7ba]/[0.06] p-5"><ShieldCheck className="size-5 text-[#71d7ba]" /><p className="mt-3 text-2xl font-semibold">{passed}/{phaseNineTenChecklist.length}</p><p className="mt-1 text-xs text-[#71809a]">gates passed</p></div><div className="rounded-2xl border border-white/[0.08] bg-[#101722] p-5"><p className="text-2xl font-semibold">{phaseNineTenChecklist.filter(item => item.status === 'pending').length}</p><p className="mt-1 text-xs text-[#71809a]">pending review</p></div><div className="rounded-2xl border border-[#d9b878]/20 bg-[#d9b878]/[0.05] p-5"><p className="text-2xl font-semibold">{phaseNineTenChecklist.filter(item => item.status === 'blocked').length}</p><p className="mt-1 text-xs text-[#71809a]">blocked gates</p></div></div><section className="mt-6 rounded-2xl border border-white/[0.08] bg-[#101722] p-5"><h2 className="font-medium">Gate checklist</h2><div className="mt-4 space-y-2">{phaseNineTenChecklist.map(item => <div key={item.id} className="flex items-center justify-between gap-4 rounded-xl border border-white/[0.06] px-4 py-3"><div><p className="text-sm text-[#d8e0eb]">{item.label}</p><p className="mt-1 text-[10px] uppercase tracking-wider text-[#71809a]">{item.required ? 'required' : 'optional'} · {item.status}</p></div>{item.status === 'pass' ? <CheckCircle2 className="size-4 text-[#71d7ba]" /> : <CircleAlert className="size-4 text-[#d9b878]" />}</div>)}</div></section></div></PageFrame>
}
